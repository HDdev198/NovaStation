
document.addEventListener('DOMContentLoaded',()=>{const c=cart(); const total=c.reduce((s,i)=>s+i.price*i.qty,0); document.querySelector('#orderSummary').innerHTML=c.map(i=>`<p>${i.title} x ${i.qty} <b>${money(i.price*i.qty)}</b></p>`).join('')||'<p>Không có sản phẩm.</p>'; document.querySelector('#checkoutTotal').textContent=money(total); document.querySelector('#checkoutForm').addEventListener('submit',e=>{e.preventDefault(); const u=JSON.parse(localStorage.getItem('novastation_user')||'{}'); const orders=JSON.parse(localStorage.getItem('novastation_orders')||'[]'); orders.unshift({id:'#NS'+Date.now().toString().slice(-6),date:new Date().toLocaleDateString('vi-VN'),total,status:'Đã thanh toán', customerName: u.name || 'Khách vãng lai', customerEmail: u.email || ''}); localStorage.setItem('novastation_orders',JSON.stringify(orders)); saveCart([]); showToast('Đặt hàng thành công!'); setTimeout(()=>location.href='orders.html',1000);});});
