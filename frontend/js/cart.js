
function renderCart(){const c=cart(); const box=document.querySelector('#cartItems'); const total=c.reduce((s,i)=>s+i.price*i.qty,0); if(!c.length){box.innerHTML='<div class="card summary-box">Giỏ hàng đang trống.</div>'; document.querySelector('#cartTotal').textContent=money(0); return;} box.innerHTML=c.map(i=>`<div class="cart-row"><img src="${i.image}" alt="${i.title}"><b>${i.title}</b><span>${money(i.price)}</span><div class="qty"><button onclick="changeQty(${i.id},-1)">-</button><b>${i.qty}</b><button onclick="changeQty(${i.id},1)">+</button></div><button class="btn" onclick="removeItem(${i.id})">Xóa</button></div>`).join(''); document.querySelector('#cartTotal').textContent=money(total);} function changeQty(id,d){let c=cart(); const it=c.find(x=>x.id==id); if(!it)return; it.qty+=d; if(it.qty<=0)c=c.filter(x=>x.id!=id); saveCart(c); renderCart();} function removeItem(id){saveCart(cart().filter(x=>x.id!=id)); renderCart();} document.addEventListener('DOMContentLoaded',renderCart);
